# Agent Rules

1. **No Browser Verification**: Never attempt to verify if the application or any feature is working using the browser tool. The user will handle all live verification.
2. **No Build/Automated Tests**: Do not run build commands (`npm run build`) or automated test suites unless explicitly requested.
3. **Follow STANDARDS.md**: Always adhere to the architectural and design standards defined in `.agent/STANDARDS.md`.
