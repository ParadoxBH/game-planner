# Endereço do backend (Spring Boot, pasta backend/).
# Sem esta variável o front usa http://localhost:8080.
# Para mudar localmente, copie para .env.local — arquivos *.local ficam fora do Git.
VITE_API_URL=http://localhost:8080
